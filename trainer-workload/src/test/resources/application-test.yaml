spring:
  activemq:
    broker-url: vm://localhost?broker.persistent=false
  mongodb:
    database: trainer-workload-test
    username: ~
    password: ~
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
security:
  jwt:
    secret: testJwtSecretForIntegrationTestsOnlyThirtyTwoCharacters

logging:
  level:
    com:
      epam:
        trainerworkload:
          level: DEBUG